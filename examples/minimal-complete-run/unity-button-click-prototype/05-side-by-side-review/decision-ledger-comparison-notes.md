# Unity Decision Ledger Comparison Notes

The Unity sample demonstrates a common Stage 3 and Stage 4 distinction:

```text
Text artifacts can be fabricated.
Unity Editor artifacts require Unity Editor creation or validation.
```

The key finalization decision is:

```text
S4-FIN-001 selected External Validation Required.
```

This prevents the example from overclaiming Unity compilation, import, test execution, scene creation, build generation, or release status.
