# Decision Ledger Comparison Notes

This miniature run uses Medium-Stakes mode at all four stages. Low-stakes formatting decisions are summarized, while medium decisions such as product type, scope, work package granularity, file fabrication method, and final handoff status are line-item logged.

The most important comparison point is the finalization decision:

```text
S4-FIN-001 selected Ready with Warnings rather than Ready because tests were not executed.
```

This demonstrates the repository's validation honesty rule: fabricated test files are not the same as executed tests.
