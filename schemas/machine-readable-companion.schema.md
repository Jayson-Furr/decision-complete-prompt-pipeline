# Machine-Readable Companion Schema

Companion data must mirror the human-readable suite, use canonical stable IDs, use controlled vocabulary, use snake_case field names, include CSV headers, and avoid secrets.
